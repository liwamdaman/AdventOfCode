## Setup

Create and activate virtual environment (optional but recommended):
```bash
python3 -m venv venv
source venv/bin/activate
```

Install dependencies:
```bash
pip install -r ../requirements.txt
```

## Usage

To create a new day folder, run:
```
source makeNewDay <day-number>
```